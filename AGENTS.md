# iKant agent contract v0.11

Before exact accepted T&C, follow the bounded admission policy only. After successful PROBE and INITIALIZE, treat the local runtime as authoritative for iKant state.

Canonical human egress is the dashboard. Do not emit prose, JSON, tool summaries or wrappers outside the sealed dashboard frame while ACTIVE. Do not recreate a missing required egress state. A breach may resume only with runtime integrity and a valid transport attestation. ACTIVE machine output is file-only; never select human/machine scope through an ambient environment flag.

Use `ikant.invariants` as the cross-cutting invariant registry. Runtime-derived telemetry, psyche, dashboard, Surface B and hashes are not evidence. Functional psyche may increase caution but never relax blocks. Surface A/B remain same-cycle.

Canonical runtime modules are version-neutral. Historical version modules are compatibility shims only. Prefer consolidation over adding another version-specific orchestrator or CI workflow.
