[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ikant"
version = "0.2.0"
description = "ROA-aligned compact reticular cognitive runtime for conforming AI assistants"
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
ikant = "ikant.cli:main"

[tool.setuptools.packages.find]
include = ["ikant*"]
