# Post-S17 runtime chain — convergence contract

Baseline: merged PR50, `main@308a601f9907dd77507514935f4b0494b2b2f9a2`. Product Contract convergence remains S17.

## Result

The post-S17 chain is a dependency DAG, not merely a numbered list:

`S17 -> S17bis -> {S18 Memory, S19 Temporal} -> S20 Intent/Plan Reconciliation -> S21 Hybrid Opt-in`

S18 and S19 are commutable siblings. Numbering records intended merge order; only the DAG edges express causal dependency.

## Why S17bis exists

Direct S17 -> S18 is falsified as prerequisite-incomplete. Memory governance would otherwise inherit restart/work reconstruction and independent surface-census responsibilities that are not memory semantics. The Development Continuity Bundle was also stale after merged S17, and CI receipts conflated PR head with GitHub's synthetic merge SHA. S17bis closes these cross-cutting foundations before new user-governable controls appear.

### S17bis — Runtime Recovery / Surface Census / Control-Plane Reconciliation

**Runtime:** reconstruct derivative WorkStore/surface presentation from canonical durable pending-frame/ACK/epoch state after restart; independently census semantic routes/composed controls/real DOM against the all-and-only manifest; type PRE_ACTIVE_BOOTSTRAP -> ACTIVE_CANONICAL; keep Git/Product Contract/bundle baseline consistent; emit true HEAD and MERGE receipt identities.

**User:** no new faculty. Recovery must converge to the exact prior RUNNING/SEALED/IDLE semantics or a bounded degraded state; never ghost progress, duplicate TURN, or parallel semantic fallback.

**Supply:** S8 persisted frame/ACK, S15 derivative WorkStore, S16 Surface Contract/PWA, S17 epoch, Chromium/CDP, Git history and GitHub Actions event context.

**DoD:** deterministic/idempotent restart reducer; crash matrix at intake/RUNNING/sealed/pre-ACK/post-ACK/epoch-rebase; real Chromium canonical-fault + DOM census; bundle/product/Git reconciliation; explicit HEAD/MERGE receipts; existing Hosted/Reticular/Product Boundary green on one synthetic merge. Branch protection is an external admin condition for any protected-merge claim and remains truthfully false until configured.

**Metrics:** 0 duplicate TURNs, 0 stale-epoch stable-base reuse, 0 undeclared semantic controls/routes, 0 mislabeled receipt SHAs, 100% bundle baseline reconciliation.

## S18 — Memory Governance

Forget is a support-aware impact closure. Preview exact consequences, preserve independently supported nodes, commit via exact confirmation, persist/replay the closure, never alter evidence scores, and never call cognitive forgetting physical erasure. Metrics: zero forgotten retrievals, zero unsupported active derivations, 100% independent support preserved.

## S19 — Temporal Task Governance

Expose S6 tasks/wakes without upgrading time to authority. Bind creation/current runtime provenance, make pause/cancel/retry user-legible, and state physical residency exactly: without a later native resident host, iKant must be running. Metrics: zero wake-to-material-action without fresh authority, zero duplicate delivery, idempotent cancellation.

## S20 — Reactive Intent / Plan Reconciliation

S15 becomes zero-authority IntentEnvelope input. Reconcile into the existing canonical action-ledger/planning/execution path as MATCH/DEMOTE/BLOCK. Never create a second planner and never turn `OPEN_APP` into execution until a real `native.app.open` adapter exists. Metrics: zero S15-to-driver bypasses, 100% material intents reconciled.

## S21 — Hybrid Abstract Assist Opt-in

Last and disabled until explicit opt-in, no-redirect effective-host pinning, strict provider-output allowlist, secret lifecycle, privacy/cost/time/token budget and provider E2E exist. Provider results remain zero-authority. Metrics: zero redirects, zero tool/unknown outputs accepted, zero private/material units exported.

## Modeled falsification

10,000,000 multi-abstraction scenarios, 96 families across development truth, surface, epoch, recovery, memory, temporal, plan, hybrid and UX/CI; two independent fault families per case; 12,288/12,288 signatures; all 45 domain pairs observed; +100,000 tail with 0 novelty. This is declared model coverage, not measured production reliability or 10M physical executions.
