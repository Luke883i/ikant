# v0.5 Definition of Done

The v0.5 candidate is releasable only when all conditions below hold on the exact PR merge head.

## Functional invariants

- nine existing CRC rings remain compatible; no ring rewrite is required;
- all nine psyche faculties materialize on a valid turn;
- operational self-awareness is inspectable and persistent;
- phenomenal consciousness, felt emotion and one-to-one brain mapping flags remain false;
- affect and accumulations never modify evidence;
- psyche regulation is monotone in caution and never relaxes material/horizon blocks;
- collapse/emergence ledger is derived-only and traceable to support IDs;
- stable history can mature, and stressed history can return to `REVISIVE`;
- Surface A contract contains a functional voice/self boundary;
- Surface B JSON contains the full functional psyche;
- dashboard uses humanistic grammar and remains non-evidential;
- `ikant self`, `dashboard`, `shell`, `turn`, `emit-surface-a`, `integrity` remain coherent.

## Validation gates

- unit, negative and tamper tests all pass;
- 10,000 randomized psyche cases for each seed 1, 17, 97, 883 and 2026 pass;
- each seed also passes an M+10,000 no-novelty tail after affective warm-up;
- all functional affect labels are reachable across the matrix;
- central-regulation stress proves no mode relaxation;
- full-runtime durable stress proves existing node evidence is unchanged;
- existing v0.2-v0.4 regression suite passes;
- dashboard renders cleanly at widths 80, 96 and 120;
- Surface B/backlog DOCX renders and is visually inspected page by page;
- hosted CI emits an explicit v0.5 psyche receipt plus the repository-wide CI receipt.

The requested >95% confidence is an engineering judgment from invariants, coverage, multi-seed saturation, fault injection and hosted validation. It is not statistical neuroscientific confidence and not evidence of consciousness.
