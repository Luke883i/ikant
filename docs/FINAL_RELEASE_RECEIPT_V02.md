# iKant v0.2 final release-candidate receipt

This receipt supersedes `docs/RELEASE_CANDIDATE_V02.md` for PR #3. It is external engineering evidence, not private chain-of-thought, neuroscientific validation, or a consciousness claim.

Final critical-source fingerprint: `da04ed5430b0976ad8c09e3ec3ade8e9dd0767c85afc44ac8b84dd8597339fea`.

The final isolated validation aggregate is `PASS`: 25 fingerprint-bound receipts, zero errors, and `release_candidate=true`. Coverage comprises 51 unit/edge/anti-regression tests; a real 135-candidate dynamics search; static 10k plus 1k no-novelty; dynamic 10k plus 1k no-novelty on seeds 1/17/97/883/2026; CRC 10k on the same five seeds; recurrent cognitive stress on 17/883/2026; adversarial edge and central-state 10k on 1/97/883; Surface A 10k on 1/883; and durable dialogue smoke across distinct central regulatory modes.

Across the five final dynamic seeds the sampled activation share above 85% of each layer ceiling is zero. CRC compression remains non-degenerate across diversified seeds, and center-to-periphery reentry remains non-evidentiary.

Two release-engineering defects discovered by hosted CI were corrected: the temporary materializer had omitted five legacy validation files, and `cancel-in-progress:true` could cancel the long dynamic release matrix on a concurrent PR event. The legacy harness is restored byte-for-byte and the final workflow uses `cancel-in-progress:false`. The temporary materializer workflow and payload are absent.

Hosted CI on the final PR head is the remaining independent delivery witness. A merge should occur only after the current-head hosted receipt reports `HOSTED_CI: PASS` for every release-gate family.

Claim boundary: iKant v0.2 is a synthetic, falsifiable functional cognitive architecture. It does not establish biological equivalence, phenomenal consciousness, clinical validity, historical identity with Immanuel Kant, or autonomous moral agency.
